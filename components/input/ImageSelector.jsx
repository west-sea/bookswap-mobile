import { View, Text, StyleSheet } from "react-native";

export default function ImageSelector() {
  return (
    <View style={styles.container}>
      {/* Image selector (TODO) */}
      <Text>Image selector will be here</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    padding: 5,
  },
});
